# BuildFROM rust:1-bookworm AS buildWORKDIR /appCOPY . .# Speed up buildsRUN cargo build -p vectradb-server --release# RuntimeFROM debian:bookworm-slimRUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*WORKDIR /appCOPY --from=build /app/target/release/vectradb-server /usr/local/bin/vectradb-serverEXPOSE 8080# Default: 384-dim, HNSW, default data dirENTRYPOINT ["vectradb-server"]