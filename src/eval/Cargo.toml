[package]
name = "vectradb-eval"
version = "0.1.0"
edition = "2021"
description = "Evaluation metrics for VectraDB: recall, precision, MRR, NDCG, MAP"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
